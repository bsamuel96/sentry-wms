# Server-only Vercel variable. Do not prefix it with VITE_ and do not commit its real value.
# Use the public origin of the separately deployed Sentry WMS API, without /api.
SENTRY_API_URL=https://sentry-api.example.com
